[build]
  command = "npm run build"
  publish = "dist/client"

# SPA fallback: every route serves the prerendered shell, and the TanStack
# Router takes over on the client.
[[redirects]]
  from = "/*"
  to = "/_shell.html"
  status = 200
